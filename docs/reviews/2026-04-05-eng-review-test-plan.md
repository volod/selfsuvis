# Test Plan
Generated by /plan-eng-review on 2026-04-05
Branch: main
Repo: selfsuvis

## Affected Pages/Routes
- No user-facing routes changed — internal pipeline test coverage only

## Key Interactions to Verify
- All 7 enrichment passes produce correct output in frame_records after indexing a new video
- Florence captioning: captions appear in search result payloads after indexing
- Detection: verify batch size is configurable via DETECTION_BATCH_SIZE env var
- Qwen bad path: when a frame file is missing during Qwen pass, prior OCR/depth data is not erased

## Edge Cases
- OOM during Florence batch — empty captions written, not NULL
- Qdrant down during set_payload — indexing completes, captions missing from search display only
- Missing frame file during any enrichment pass — blank PIL image used, pass continues
- World model: partial last window (fewer frames than clip_size)

## Critical Paths
- index_video() → ASR → Florence → OCR → Qwen → Depth → Detection → World Model (full sequence)
- Qwen file_error must not erase OCR data written in prior pass
- Detection batch size override: DETECTION_BATCH_SIZE=2 → batches of 2, no records skipped or double-processed
