python-dotenv==1.0.1
fastapi>=0.115.0
uvicorn[standard]>=0.27.1
streamlit>=1.37.0
ninja>=1.13.0
torch==2.10.0
torchvision==0.25.0
xformers==0.0.35
open-clip-torch==2.24.0
ultralytics>=8.3.0
sam3
sam2
segment-anything==1.0
rfdetr>=1.1.0
# torchsig PyPI releases cap at Python ≤3.11; install from git for Python 3.12+:
#   pip install git+https://github.com/torchdsp/torchsig.git
# rf_analyzer.py falls back to scipy/numpy spectrograms when torchsig is absent.
# torchsig @ git+https://github.com/torchdsp/torchsig.git
pillow>=10.2.0
opencv-python==4.10.0.84
numpy>=1.26.4
scikit-image>=0.26.0
scikit-learn>=1.4.0
plyfile>=0.9
# 3D Gaussian Splatting — CUDA JIT kernels compiled on first use.
# Requires CUDA-enabled torch.  CPU-only machines skip gsplat gracefully.
gsplat>=1.4.0
qdrant-client==1.8.0
asyncpg>=0.29.0
python-multipart>=0.0.9
pydantic>=2.8.0
tqdm==4.66.2
faiss-cpu==1.8.0
onnxscript>=0.1
pycolmap==4.0.2
# onnxruntime: CPU-only base install.  On CUDA machines scripts/install_requirements.sh
# upgrades this to onnxruntime-gpu (which also bundles TensorrtExecutionProvider).
# Do NOT install both onnxruntime and onnxruntime-gpu in the same environment.
onnxruntime>=1.18.0
requests==2.31.0
transformers>=4.47.0,<4.54.0
accelerate>=0.26.0
# flash-attn is NOT listed here — it requires torch to be present at build time, so it
# cannot be installed in a single `pip install -r` pass alongside torch.
# It is installed automatically by scripts/install_requirements.sh (make venv) after torch,
# and manually via:  python scripts/prepare_models.py --flash-attn
openai>=1.0
google-genai>=1.0
# vllm is a separate inference server (run via Docker or standalone process), not a Python import.
# Install it independently if needed: pip install vllm  (requires its own CUDA env).
httpx>=0.27.0
# ASR — Whisper and faster alternatives
faster-whisper>=1.0.0
sentencepiece>=0.1.99
# Model registry resource detection
psutil>=5.9.0
# GOT-OCR2_0 support
tiktoken>=0.6.0
verovio>=3.12.0
