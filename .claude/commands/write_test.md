Write comprexencive tests for $ARGUMENTS

Testing conventions:

- use pytest library
- put test files into **tests** directory in the same folders as the source files

Tests coverage:

- Test happy path
- Test edge cases
- test error states
- Focus on algorithms correctness and public API behaviour rather than implementation details